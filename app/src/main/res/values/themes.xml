<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Tema para la Splash Screen -->


    <!-- Tema base de la aplicación que hereda de AppCompat -->
    <style name="Theme.VibeTurn" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Colores principales -->
        <item name="colorPrimary">#6200EE</item>
        <item name="colorPrimaryDark">#3700B3</item>
        <item name="colorAccent">#03DAC5</item>
        <!-- Personalización adicional -->
        <item name="android:statusBarColor">?attr/colorPrimaryDark</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowIsTranslucent">true</item>
    </style>

    <!-- Puedes agregar un tema oscuro heredando de AppCompat si lo deseas -->
    <style name="Theme.VibeTurn.Dark" parent="Theme.AppCompat.DayNight.NoActionBar"/>
    <style name="SplashTheme" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="android:windowBackground">@drawable/splash</item>
        <item name="android:windowIsTranslucent">true</item>
    </style>
</resources>